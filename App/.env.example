DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME
PORT=5000

# Optional: point the dashboard at the FastAPI MT5 backend instead of same-origin routes.
VITE_API_BASE_URL=http://localhost:5000

# Optional: read-only key for protected MT5 read endpoints.
VITE_MT5_READ_API_KEY=
